// Import the Component decorator from Angular core
import { Component } from '@angular/core';

// Define the DashboardComponent with its metadata
@Component({
  selector: 'app-dashboard', // The component's selector
  templateUrl: './dashboard.component.html', // Path to the component's template
  styleUrls: ['./dashboard.component.scss'], // Path to the component's styles
})
export class DashboardComponent {
  // Component logic goes here (currently empty)
}
